# config/services.yaml
services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false

    Markocupic\ContaoFlashMessage\:
        resource: ../src/
        exclude: ../src/{DependencyInjection,Model,Session}
